/**
 * Known wallet labels are maintained in `knownWallets.ts` as read-only,
 * version-controlled configuration.
 */
export const MY_WALLETS_INFO = 'Known wallet labels are maintained in src/config/knownWallets.ts.';
