/**
 * Read-only wallet labels used by server-side analysis.
 *
 * Add entries through code review. Keys must be complete lowercase EVM
 * addresses and labels must be plain, single-line text.
 */
export const KNOWN_WALLETS: Readonly<Record<string, string>> = Object.freeze({});
